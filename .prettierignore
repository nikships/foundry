out
dist
node_modules
coverage
package-lock.json
assets
*.icns
*.png
*.svg
.foundry-worktrees
.codegraph
playwright-report
test-results

# Vendored copies of @earendil-works/pi-coding-agent docs. Do not reformat.
references

# Standalone marketing site — has its own toolchain and is not part of the
# desktop app build, the repo gate, or CI. See apps/website/README.md.
apps/website
website

# Prose and agent scaffolding, not app source. Hand-tuned Markdown here
# (tables, prompt bodies, historical plans) should stay exactly as written, and
# a formatter diff on it is noise that fails the gate for no correctness gain.
# `specs` and `.factory/docs` are already excluded from check:docs for the same
# reason: they are records of the repo as it was, not living documentation.
.agents
.factory
specs

# Amp plugins and agent modes. These files are pulled down verbatim from
# ampcode.com and refreshed in place, so they carry upstream's formatting
# (tabs, no semicolons) rather than this repo's. Reformatting them would be
# undone by the next sync.
.amp

# Android application scaffold
apps/android
android

