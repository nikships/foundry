name: CI

# Agent-ready verification for the Foundry desktop app.
# Public repo: free GitHub-hosted minutes. All jobs run on GitHub runners.

on:
  push:
    branches: [main]
    paths:
      - 'apps/**'
      - 'scripts/**'
      - 'assets/**'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - 'electron.vite.config.ts'
      - 'electron-builder.yml'
      - '*.config.*'
      - '.gitattributes'
      - '.githooks/**'
      - '.jscpd.json'
      - '.github/workflows/**'
  # No paths filter here, on purpose. verify, android, and actionlint are required checks
  # on main, and a required check that never starts is never satisfied: a PR
  # touching only docs or assets is then blocked forever, and even an admin
  # merge is refused. All required jobs are cheap enough to run on every PR.
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

defaults:
  run:
    shell: bash

jobs:
  # ── Quality gates (types / lint / format / dead code / tests / build / audit)
  verify:
    name: verify
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 12
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Typecheck
        run: pnpm run typecheck

      - name: Lint
        run: pnpm run lint

      - name: File size guardrails
        run: pnpm run check:files

      - name: Duplicate code
        run: pnpm run check:duplicate

      - name: Format
        run: pnpm run format:check

      - name: Documented commands
        # Static check that the commands in AGENTS.md / README / Makefile /
        # workflows still resolve to real pnpm scripts and make targets.
        run: pnpm run check:docs

      - name: Dead code (knip)
        run: pnpm run knip

      - name: Test
        # Coverage thresholds live in vitest.config.ts, so the local gate
        # (pnpm run check) and CI enforce exactly the same floor.
        run: pnpm run test:coverage

      - name: Build
        run: pnpm run build

      - name: Dependency audit
        # Fails on high/critical only so moderate noise does not block PRs.
        run: pnpm run audit:deps

  # ── Android unit tests ─────────────────────────────────────────────────────
  android:
    name: android
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Java
        uses: actions/setup-java@v6
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: gradle

      - name: Run Android unit tests
        working-directory: apps/android
        run: ./gradlew :app:testDebugUnitTest

  # ── Workflow hygiene ───────────────────────────────────────────────────────
  actionlint:
    name: actionlint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Download actionlint
        run: |
          set -euo pipefail
          # 1.7.12+ knows macos-26; older pins reject it as an unknown label.
          bash <(curl -fsSL https://raw.githubusercontent.com/rhysd/actionlint/main/scripts/download-actionlint.bash) 1.7.12
          ./actionlint -version

      - name: Lint workflows
        run: |
          set -euo pipefail
          ./actionlint -shellcheck=

  # ── Electron UI smoke (advisory) ───────────────────────────────────────────
  # Playwright launches the built app against isolated fixtures. Needs a real
  # macOS window server, so it is NOT part of `pnpm run check` and is not a
  # required status check. Promote once the flake rate is known.
  e2e:
    name: e2e
    runs-on: macos-26

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 12
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install
        run: pnpm install --frozen-lockfile

      - name: Build
        run: pnpm run build

      - name: Electron UI smoke
        run: pnpm run test:e2e

      - name: Upload Playwright artifacts
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: e2e-playwright
          path: |
            playwright-report
            test-results
          if-no-files-found: ignore
